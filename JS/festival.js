const swiper = new Swiper(".swiper", {
  // And if we need scrollbar
  scrollbar: {
    el: ".swiper-scrollbar",
    hide: true,
    clickable: true,
  },
  spaceBetween: 30,
  pagination: {
    el: ".swiper-pagination",
    clickable: true,
  },
});
